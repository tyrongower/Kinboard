// Environment configuration
// Use process.env for Expo environment variables

// Intentionally left for future environment-driven, non-sensitive build-time settings.
// API server configuration is runtime-configured and stored per device (see `src/api/apiBaseUrl.ts`).
